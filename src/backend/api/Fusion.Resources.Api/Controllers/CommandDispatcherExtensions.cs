using Fusion.Resources.Database.Entities;
using Fusion.Resources.Domain;
using System;
using System.Threading.Tasks;

namespace Fusion.Resources.Api.Controllers
{
    public static class CommandDispatcherExtensions
    {
        //public static async Task<QueryPersonnelRequest> UpdateState(this ResourceControllerBase.CommandDispatcher dispatcher, Guid requestId, DbRequestState state)
        //{
        //    var item = await dispatcher.DispatchAsync(new Domain.Commands.UpdateContractPersonnelRequest(requestId) { State = state });
        //    return item;
        //}
    }
}
