namespace Fusion.Resources.Domain
{
    public static class ReasonRoles
    {
        public enum Roles
        {
            DelegatedManager,
            DelegatedParentManager,
            ParentManager,
            Manager,
            Write
        }
    }
}
