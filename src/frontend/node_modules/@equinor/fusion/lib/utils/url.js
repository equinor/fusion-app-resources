export const trimTrailingSlash = (url) => url.replace(/\/$/, '');
export const sanitizedUrl = (url) => trimTrailingSlash(url.replace(/(\w:\/\/)|(\/)+/g, '$1$2'));
export const combineUrls = (base, ...parts) => sanitizedUrl([base]
    .concat(parts)
    .filter((a) => !!a)
    .join('/'));
