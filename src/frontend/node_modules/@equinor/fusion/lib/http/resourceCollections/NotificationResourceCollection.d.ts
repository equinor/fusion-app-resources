import BaseResourceCollection from './BaseResourceCollection';
export default class NotificationResourceCollection extends BaseResourceCollection {
    protected getBaseUrl(): string;
    notification(id: string): string;
    personNotifications(personIdentifier: string, filter?: string): string;
    personNotificationSettings(identifier?: string): string;
}
