import BaseResourceCollection from './BaseResourceCollection';
import { ContextTypes } from '../apiClients/models/context';
export default class ContextResourceCollection extends BaseResourceCollection {
    protected getBaseUrl(): string;
    apiSignin(): string;
    contexts(): string;
    context(id: string): string;
    queryContexts(query: string, ...types: ContextTypes[]): string;
    relatedContexts(id: string, ...types: ContextTypes[]): string;
}
