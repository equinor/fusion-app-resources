import { Observable } from 'rxjs';
/**
 * Hook selector for attribute on a observable
 *
 * @param state$
 * @param property
 * @param options
 */
export declare const useObservableSelector: <S, P extends keyof S>(state$: Observable<S>, property: P, options?: {
    compare?: ((x: S[P], y: S[P]) => boolean) | undefined;
    initial?: S[P] | undefined;
} | undefined) => S[P] | undefined;
export default useObservableSelector;
