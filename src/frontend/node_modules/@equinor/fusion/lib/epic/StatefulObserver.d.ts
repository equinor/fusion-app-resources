import { Observable } from 'rxjs';
export declare class StatefulObserver<S> extends Observable<S> {
    value: S;
    private __notifier;
    constructor(input$: Observable<S>, initialState: S);
}
export default StatefulObserver;
