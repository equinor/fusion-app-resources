import { Position } from '@equinor/fusion';
declare const usePositionQuery: (selectedPosition: Position | null, projectId: string, contractId?: string) => [Error, boolean, Position[], (query: string) => void];
export default usePositionQuery;
