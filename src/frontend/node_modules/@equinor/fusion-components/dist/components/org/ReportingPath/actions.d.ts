import { OrgStructure } from '@equinor/fusion-components';
import { ReportingPathProps } from '.';
declare type ReportingPathActionProps<T> = ReportingPathProps<T> & {
    parentHeight: number;
    parentWidth: number;
};
declare const useReportingPathActions: <T extends OrgStructure>({ structure, cardWidth, cardHeight, cardMargin, rowMargin, component, parentHeight, parentWidth, }: ReportingPathActionProps<T>) => void;
export default useReportingPathActions;
