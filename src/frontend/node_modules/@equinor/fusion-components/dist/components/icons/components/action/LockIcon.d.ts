/// <reference types="react" />
import { IconProps } from '@equinor/fusion-components';
declare type LockIconProps = {
    outline: boolean;
} & IconProps;
declare const LockIcon: ({ outline, ...rest }: LockIconProps) => import("react").ReactSVGElement;
export default LockIcon;
