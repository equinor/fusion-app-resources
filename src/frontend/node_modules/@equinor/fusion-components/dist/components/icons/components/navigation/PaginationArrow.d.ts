/// <reference types="react" />
import { IconProps } from '@equinor/fusion-components';
declare type PaginationArrowIconProps = {
    next?: boolean;
    prev?: boolean;
} & IconProps;
declare const PaginationArrow: ({ next, prev, ...props }: PaginationArrowIconProps) => import("react").ReactSVGElement;
export default PaginationArrow;
