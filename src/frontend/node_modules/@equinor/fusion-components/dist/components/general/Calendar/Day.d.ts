import { CalendarDate } from '@equinor/fusion';
import { FC } from 'react';
declare type DayProps = {
    date: CalendarDate;
    interactive?: boolean;
    selectedDate?: Date | null;
    onClick?: (date: Date) => void;
};
declare const Day: FC<DayProps>;
export default Day;
