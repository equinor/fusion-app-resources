import { ReactNode } from 'react';
import { NavigationStructure } from '@equinor/fusion-components';
declare type NavigationPopoverProps = {
    icon?: ReactNode;
    isActive?: boolean;
    navigationStructure?: (JSX.Element | null)[] | null;
    navigationChildren?: NavigationStructure[];
    groupingComponent?: () => JSX.Element;
};
declare const NavigationPopover: ({ icon, navigationStructure, groupingComponent, isActive, navigationChildren, }: NavigationPopoverProps) => JSX.Element;
export default NavigationPopover;
