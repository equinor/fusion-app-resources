import { FC } from 'react';
declare type StepPaneProps = {
    onChange: (stepKey: string) => void;
    children: any;
    activeStepKey: string;
    activeStepPosition: number;
    forceOrder: boolean;
};
declare const StepPane: FC<StepPaneProps>;
export default StepPane;
