/// <reference types="react" />
import { FilterTerm, Filter } from '../applyFilters';
declare type SearchFilterProps<T> = {
    term: FilterTerm;
    onChange: (value: string) => void;
    quickFactScope?: string;
    filter: Filter<T>;
};
declare function SearchFilterComponent<T>({ filter, term, onChange, quickFactScope, }: SearchFilterProps<T>): JSX.Element;
export default SearchFilterComponent;
