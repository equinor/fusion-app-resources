/// <reference types="react" />
import { FilterTerm, Filter as FilterConfig } from '../applyFilters';
import { Count } from '../countFilters';
declare type FilterProps<T> = {
    filter: FilterConfig<T>;
    term?: FilterTerm;
    filterCount: Count[];
    onChange: (filter: FilterConfig<T>, term: FilterTerm | null) => void;
    quickFactScope?: string;
};
declare function Filter<T>({ filter, term: defaultTerm, filterCount, onChange, quickFactScope, }: FilterProps<T>): JSX.Element;
export default Filter;
