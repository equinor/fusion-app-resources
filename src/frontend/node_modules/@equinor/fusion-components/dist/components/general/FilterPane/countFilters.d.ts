import { FilterTerm, FilterOptions, FilterSection } from './applyFilters';
export declare type FilterCount = {
    key: string;
    count: number;
};
export declare type Count = {
    key: string;
    count: FilterCount[] | number;
};
export declare type OnFilterCountChangeHandler = (count: Count[]) => void;
declare const countFiltersAsync: <T>(data: T[], options: FilterOptions<T>) => Promise<Count[]>;
export declare const applyCountAsync: <T>(data: T[], sectionDefinitions: FilterSection<T>[], terms: FilterTerm[], setFilterCount: OnFilterCountChangeHandler, abortSignal: AbortSignal) => Promise<void>;
export default countFiltersAsync;
