import { FC, MutableRefObject, ReactNode } from 'react';
export declare type DropdownController = {
    isOpen: boolean;
    setIsOpen: (isOpen: boolean) => void;
    node: ReactNode;
    controllerRef: MutableRefObject<HTMLElement | null>;
};
declare type DropdownJustification = 'left' | 'right';
declare type DropdownProps = {
    controller: DropdownController;
    justification?: DropdownJustification;
};
export declare const useDropdownController: (controller: (ref: MutableRefObject<HTMLElement | null>, isOpen: boolean, setIsOpen: (isOpen: boolean, delay?: number) => void) => ReactNode) => DropdownController;
declare const Dropdown: FC<DropdownProps>;
export default Dropdown;
