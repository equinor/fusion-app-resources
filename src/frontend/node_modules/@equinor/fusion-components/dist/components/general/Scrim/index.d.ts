import { FC, ReactNode, MouseEvent } from 'react';
declare type ScrimProps = {
    children: ReactNode;
    show?: boolean;
    onClick?: (event: MouseEvent<HTMLDivElement>) => void;
};
declare const Scrim: FC<ScrimProps>;
export default Scrim;
