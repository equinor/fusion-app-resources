import { AppManifest, BookmarkListResponse } from '@equinor/fusion';
import { BookmarkView } from '../../types';
declare type AllBookmarksProps<TPayload> = {
    allBookmarks: BookmarkListResponse[] | undefined;
    onClose: () => void;
    currentApp: AppManifest | null;
    capturePayload: () => Promise<TPayload>;
    onViewChanged?: (view: BookmarkView) => void;
    hasContext: boolean;
};
export declare const SideSheetManager: <T extends unknown>({ allBookmarks, currentApp, onClose, capturePayload, onViewChanged, hasContext, }: AllBookmarksProps<T>) => JSX.Element;
export default SideSheetManager;
