/// <reference types="react" />
import { PhotoSize } from '@equinor/fusion-components';
declare type FallbackImageProps = {
    size: PhotoSize;
    rotation?: boolean;
};
declare const FallbackImage: ({ size, rotation }: FallbackImageProps) => JSX.Element;
export default FallbackImage;
