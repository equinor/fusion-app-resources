/// <reference types="react" />
import { PhotoSize } from '@equinor/fusion-components';
import { PersonDetails } from '@equinor/fusion';
export { PhotoSize };
export declare type PersonCardProps = {
    personId?: string;
    person?: PersonDetails;
    photoSize?: PhotoSize;
    inline?: boolean;
    hidePopover?: boolean;
    showJobTitle?: boolean;
    isFetchingPerson?: boolean;
};
declare const _default: ({ personId, person, inline, photoSize, hidePopover, showJobTitle, isFetchingPerson, }: PersonCardProps) => JSX.Element;
export default _default;
