/// <reference types="react" />
import { PhotoSize } from '@equinor/fusion-components';
import { PersonDetails } from '@equinor/fusion';
declare type AccountTypeIkonProps = {
    size: PhotoSize;
    currentPerson: PersonDetails;
    hideTooltip?: boolean;
};
declare const AccountTypeIkon: ({ size, currentPerson, hideTooltip }: AccountTypeIkonProps) => JSX.Element;
export default AccountTypeIkon;
