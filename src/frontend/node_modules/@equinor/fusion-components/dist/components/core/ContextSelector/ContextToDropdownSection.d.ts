import { SearchableDropdownSection } from '@equinor/fusion-components';
import { Context } from '@equinor/fusion';
export declare const formattedContextType: (contextType: string) => string;
export default function (contexts: Context[], searchQuery: string, isQuerying: boolean, selectedContext?: Context | null): SearchableDropdownSection[];
