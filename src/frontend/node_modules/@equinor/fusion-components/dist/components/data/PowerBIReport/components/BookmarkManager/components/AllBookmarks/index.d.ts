import { BookmarkContext, PBIBookmark, UpdateBookmarkOperation } from '../../useBookmarks';
import { FC } from 'react';
declare type AllBookmarksProps = {
    allBookmarks: BookmarkContext[];
    currentContextId: string;
    updateBookmark: (bookmark: PBIBookmark, operation: UpdateBookmarkOperation, contextId: string) => void;
    onBookmarkSelect: (bookmark: PBIBookmark, contextId: string) => void;
};
declare const AllBookmarks: FC<AllBookmarksProps>;
export default AllBookmarks;
