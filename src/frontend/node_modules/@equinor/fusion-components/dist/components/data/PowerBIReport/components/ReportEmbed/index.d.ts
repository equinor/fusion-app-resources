import * as pbi from 'powerbi-client';
import { Context } from '@equinor/fusion';
import { AccessToken, EmbedInfo } from '@equinor/fusion/lib/http/apiClients/models/report/';
import { FC } from 'react';
import { PowerBIReportErrorDetail } from '../..';
declare type PowerBIProps = {
    reportId: string;
    embedInfo: EmbedInfo;
    accessToken: AccessToken;
    setError: React.Dispatch<React.SetStateAction<PowerBIReportErrorDetail | null>>;
    currentContext: Context | null;
    filters?: pbi.models.ReportLevelFilters[] | null;
    hasContext?: boolean;
};
/**
 * TODO: use native react component from Microsoft
 */
declare const ReportEmbed: FC<PowerBIProps>;
export default ReportEmbed;
