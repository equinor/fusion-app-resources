import { Plugin } from 'prosemirror-state';
import { EditorView } from 'prosemirror-view';
import { MdMenuItemType } from './menuItems';
export declare type MenuItem = {
    command: Function;
    dom: HTMLButtonElement;
    type: any;
};
export declare class MenuView {
    editorView: EditorView;
    items: MenuItem[];
    dom: Element;
    constructor(items: MenuItem[], editorView: EditorView, menuContainer: Element);
    private isMarkActive;
    protected updateMeuItem(): void;
    update(): void;
    destroy(): void;
}
export default function menuPlugin(menuContainer: any, itemTypes: Array<MdMenuItemType>): Plugin<any, any>;
