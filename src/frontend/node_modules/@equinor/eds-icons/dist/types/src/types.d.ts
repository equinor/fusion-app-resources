import * as icons from './data';
export declare type IconName = keyof typeof icons | string;
export declare type IconData = {
    name: IconName;
    prefix: string;
    height: string;
    width: string;
    svgPathData: string;
};
