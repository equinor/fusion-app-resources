import { jsx as _jsx } from "react/jsx-runtime";
import { createGenerateClassName, StylesProvider as BaseStylesProvider, } from '@material-ui/styles';
import Element from '@equinor/fusion-wc-theme';
Element;
export const StylesProvider = (props) => {
    const { children, seed, ...args } = props;
    if (seed && !args.generateClassName) {
        args.generateClassName = createGenerateClassName({ seed });
    }
    return _jsx(BaseStylesProvider, Object.assign({}, args, { children: children }), void 0);
};
export default StylesProvider;
//# sourceMappingURL=StyleProvider.js.map