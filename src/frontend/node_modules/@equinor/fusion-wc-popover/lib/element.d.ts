import { LitElement, TemplateResult } from 'lit-element';
import { Placement } from '@popperjs/core';
import { Options, PopperModifier } from './directories/popperjs';
export declare type PopoverElementProps = {
    disabled?: boolean;
    placement?: Placement;
    show?: boolean;
    flip?: boolean;
    offset?: [number, number];
    triggers?: ShowTrigger[];
};
export declare type ShowTrigger = 'hover' | 'click';
/**
 * Element wrapper for PopperJS
 */
export declare class PopoverElement extends LitElement implements PopoverElementProps {
    static styles: import("lit-element").CSSResult[];
    placement: Placement;
    disabled?: boolean;
    show?: boolean;
    flip?: boolean;
    offset?: [number, number];
    triggers: ShowTrigger[];
    popper: Promise<HTMLSpanElement>;
    get options(): Options;
    private _modifiers;
    get modifiers(): PopperModifier[];
    /** Set popper modifiers for element */
    set modifiers(modifiers: PopperModifier[]);
    render(): TemplateResult;
    protected handleClick(): void;
}
export default PopoverElement;
