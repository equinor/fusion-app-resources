"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
const template_dirs_1 = require("./template-dirs");
const root = path_1.resolve(process.cwd(), 'src', 'templates');
const dir = path_1.join(root, '.config');
const files = fs_1.readdirSync(dir).map(path => path_1.join(dir, path)).filter(path => fs_1.statSync(path).isFile());
const copyFiles = (dir) => files.forEach(file => fs_1.copyFileSync(file, path_1.join(dir, path_1.basename(file))));
template_dirs_1.default(root).forEach(copyFiles);
//# sourceMappingURL=update-template-files.js.map