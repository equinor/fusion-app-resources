import { Command, flags } from '@oclif/command';
export default class BuildApp extends Command {
    static description: string;
    static flags: {
        out: flags.IOptionFlag<string>;
        silent: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        zip: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    run(): Promise<void>;
    private buildAppAsync;
    private clearOutputDirAsync;
    private runBuildAsync;
    private generateManifestAsync;
    private writeManifestAsync;
    private copyResourcesAsync;
    private copyResourceAsync;
    private generateZipAsync;
    private createWebpackConfigAsync;
    private getOutputDirAsync;
    private parsePackageVersion;
}
