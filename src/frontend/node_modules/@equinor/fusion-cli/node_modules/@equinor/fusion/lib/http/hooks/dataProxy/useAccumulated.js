import useApiClient from '../useApiClient';
export function useAccumulatedItem(siteCode, projectIdentifier, action) {
    return useApiClient(async (apiClients) => {
        const response = await apiClients.dataProxy.getAccumulatedItemAsync(siteCode, projectIdentifier, action);
        return response.data;
    }, [siteCode, projectIdentifier]);
}
