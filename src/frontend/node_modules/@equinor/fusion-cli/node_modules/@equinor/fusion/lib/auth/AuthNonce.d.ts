import AuthApp from './AuthApp';
export declare class FusionNonceNotFoundError extends Error {
    constructor(key: string);
}
export default class AuthNonce {
    private key;
    private value;
    private static createCacheKey;
    static createNew(app: AuthApp): AuthNonce;
    static resolve(key: string): AuthNonce;
    constructor(key: string, value: string);
    toString(): string;
    getKey(): string;
    validate(nonce: AuthNonce): boolean;
}
