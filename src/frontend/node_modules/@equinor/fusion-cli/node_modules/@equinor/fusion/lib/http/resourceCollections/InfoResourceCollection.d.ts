import BaseResourceCollection from './BaseResourceCollection';
export declare class InfoResourceCollection extends BaseResourceCollection {
    protected getBaseUrl(): string;
    quickFact(scope: string, id: string): string;
    quickFacts(scope: string): string;
}
export default InfoResourceCollection;
