import PbiGroup from './models/powerbi/PowerBIGroup';
import PbiDashboard from './models/powerbi/PowerBIDashboard';
import PbiReport from './models/powerbi/PowerBIReport';
import BaseApiClient from './BaseApiClient';
import PowerBIDataset from './models/powerbi/PowerBIDataset';
export default class PowerBIClient extends BaseApiClient {
    private groups;
    private reports;
    private dashboards;
    private powerBiToken;
    protected getBaseUrl(): string;
    private getPowerBITokenAsync;
    getDatasetAsync(groupId: string, dataSet: string): Promise<PowerBIDataset>;
    getGroupsAsync(): Promise<PbiGroup[]>;
    getReportsAsync(groupId: string): Promise<PbiReport[]>;
    getDashboardsAsync(groupId: string): Promise<PbiDashboard[]>;
    private handleResponse;
    private buildRequestOptionsWithToken;
}
