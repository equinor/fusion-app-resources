import { Epic, Action } from './epic';
/**
 * Merges all epics into a single one.
 */
export declare function combineEpics<T extends Action = Action, O extends T = T, S = void, D = unknown>(...epics: Epic[]): Epic<T, O, S, D>;
