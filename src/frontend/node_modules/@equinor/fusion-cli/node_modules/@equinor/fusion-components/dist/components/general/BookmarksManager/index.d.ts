import { BookmarkPayloadResponse } from '@equinor/fusion';
declare type BookmarkPayload<TPayload> = Omit<BookmarkPayloadResponse, 'payload'> & {
    payload: TPayload;
};
export declare type BookmarksManagerProps<TPayload, TPromise> = {
    capturePayload: () => Promise<TPayload>;
    applyBookmark: (bookmarkPayload: BookmarkPayload<TPayload>, awaitForContextSwitch: boolean) => Promise<TPromise>;
    name: string;
    anchorId: string;
    hasContext: boolean;
};
export declare const BookmarksManager: <T extends unknown, TPromise extends unknown>({ capturePayload, applyBookmark, name, anchorId, hasContext, }: BookmarksManagerProps<T, TPromise>) => JSX.Element;
export default BookmarksManager;
