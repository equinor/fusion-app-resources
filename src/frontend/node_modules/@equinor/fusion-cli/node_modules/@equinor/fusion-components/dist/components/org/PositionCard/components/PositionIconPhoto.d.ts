import { FC, ReactNode } from 'react';
import { Position, PositionInstance } from '@equinor/fusion';
declare type PositionPhotoIconProps = {
    position: Position;
    currentInstance?: PositionInstance;
    isLinked?: boolean;
    onClick?: (position: Position, instance: PositionInstance) => void;
    rotationInstances: PositionInstance[];
    personPhotoComponent?: ReactNode;
    showTaskOwner?: boolean;
};
declare const PositionPhotoIcon: FC<PositionPhotoIconProps>;
export default PositionPhotoIcon;
