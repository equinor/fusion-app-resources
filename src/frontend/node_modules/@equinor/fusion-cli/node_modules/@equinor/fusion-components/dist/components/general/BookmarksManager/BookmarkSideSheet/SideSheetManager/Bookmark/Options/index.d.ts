import { BookmarkResponse } from '@equinor/fusion';
declare type OptionsProps = {
    onEdit: () => void;
    onDelete: () => void;
    onShare: (share: boolean) => void;
    onRemove: () => void;
    accordionOpen?: boolean;
    bookmarkInfo: Pick<BookmarkResponse, 'isShared' | 'createdBy'>;
};
export declare const Options: ({ onEdit, onDelete, onShare, onRemove, accordionOpen, bookmarkInfo, }: OptionsProps) => JSX.Element;
export default Options;
