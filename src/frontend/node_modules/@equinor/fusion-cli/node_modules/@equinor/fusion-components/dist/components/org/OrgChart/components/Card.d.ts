/// <reference types="react" />
import { OrgNode } from '../orgChartTypes';
declare type CardProps<T> = {
    node: OrgNode<T>;
    x?: number;
    y?: number;
};
declare function Card<T>({ node, x, y }: CardProps<T>): JSX.Element;
export default Card;
