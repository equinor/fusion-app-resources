import { OrgStructure, OrgChartItemProps } from '@equinor/fusion-components';
import { FC } from 'react';
export declare type ReportingPathProps<T> = {
    structure: T[];
    component: FC<OrgChartItemProps<T>> | null;
    cardWidth?: number;
    cardHeight?: number;
    cardMargin?: number;
    rowMargin?: number;
};
declare const ReportingPath: <T extends OrgStructure>(props: ReportingPathProps<T>) => JSX.Element;
export default ReportingPath;
