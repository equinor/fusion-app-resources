import { FilterTerm } from '../applyFilters';
import { Count } from '../countFilters';
import { FC } from 'react';
declare type CheckBoxOption = {
    key: string;
    label: string;
    color?: string;
    hideCount?: boolean;
};
declare type OnChangeHandler = (newTerms: string[] | null) => void;
declare type CheckBoxesFilterProps = {
    options: CheckBoxOption[];
    term?: FilterTerm;
    onChange: OnChangeHandler;
    filterCount: Count;
};
declare const CheckBoxesFilterComponent: FC<CheckBoxesFilterProps>;
export default CheckBoxesFilterComponent;
