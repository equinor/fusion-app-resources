/// <reference types="react" />
import { Position } from '@equinor/fusion';
declare type PositionPickerProps = {
    initialPosition?: Position;
    onSelect?: (position: Position) => void;
    projectId: string;
    contractId?: string;
    selectedPosition: Position | null;
    label?: string;
    placeholder?: string;
};
declare const PositionPicker: ({ initialPosition, selectedPosition, onSelect, projectId, contractId, label, placeholder, }: PositionPickerProps) => JSX.Element;
export default PositionPicker;
