declare type BookmarkSideSheetProps<TPayload> = {
    anchorId: string;
    capturePayload: () => Promise<TPayload>;
    isOpen: boolean;
    onClose: () => void;
    hasContext: boolean;
};
export declare const BookmarkSideSheet: <T extends unknown>({ isOpen, onClose, anchorId, capturePayload, hasContext, }: BookmarkSideSheetProps<T>) => JSX.Element;
export default BookmarkSideSheet;
