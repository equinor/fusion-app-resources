import { FC } from 'react';
import { Pagination as PaginationConfig, Page } from '@equinor/fusion';
export declare type PaginationChangeHandler = (newPage: Page, perPage: number) => void;
declare type SkeletonPaginationProps = {
    pagination: PaginationConfig;
};
declare type PaginationProps = {
    pagination: PaginationConfig;
    onChange: PaginationChangeHandler;
};
declare const Pagination: FC<PaginationProps>;
export declare const PaginationSkeleton: FC<SkeletonPaginationProps>;
export default Pagination;
