import { FilterTerm } from '../applyFilters';
import { Count } from '../countFilters';
import { FC } from 'react';
declare type RadioOption = {
    key: string;
    label: string;
    color?: string;
};
declare type RadioButtonsFilterProps = {
    options: RadioOption[];
    term: FilterTerm;
    onChange: (value: string) => void;
    filterCount: Count;
    paneIsCollapsed: boolean;
};
declare const RadioButtonsFilterComponent: FC<RadioButtonsFilterProps>;
export default RadioButtonsFilterComponent;
