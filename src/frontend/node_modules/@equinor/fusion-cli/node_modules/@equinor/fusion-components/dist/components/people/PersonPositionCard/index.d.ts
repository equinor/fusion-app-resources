import { FC } from 'react';
import { PersonPosition } from '@equinor/fusion';
declare type PersonPositionCardProps = {
    position: PersonPosition;
};
declare const PersonPositionCard: FC<PersonPositionCardProps>;
export default PersonPositionCard;
