import { FC, ReactNode } from 'react';
import { ResizablePaneOptions } from '../useResizablePanel';
declare type SideSheetSize = 'xlarge' | 'large' | 'medium' | 'small';
export declare type StandardSideSheetProps = ResizablePaneOptions & {
    id: string;
    title?: string;
    isOpen: boolean;
    onClose: (isOpen: boolean) => void;
    screenPlacement?: 'right' | 'left';
    size?: SideSheetSize;
    children: ReactNode;
    headerContent?: ReactNode;
};
declare const StandardSideSheet: FC<StandardSideSheetProps>;
export default StandardSideSheet;
