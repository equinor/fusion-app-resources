import { Position } from '@equinor/fusion';
import { SearchableDropdownOption } from '@equinor/fusion-components';
export declare const singlePositionToDropdownOption: (query: string, position: Position, selectedPosition: Position | null) => SearchableDropdownOption[];
export declare const positionsToDropdownOption: (query: string, positions: Position[], selectedPosition: Position | null) => SearchableDropdownOption[];
